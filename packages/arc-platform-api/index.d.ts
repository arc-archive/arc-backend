import { Express } from 'express';
import { Server } from 'http';

export declare const serverResolve: (value: any) => void;
export declare const server: Server;
declare const app: Express;
export default app;
